//
//  Layout.swift
//  ReWork
//
//  Created by Daniel Molodet on 27/08/2026.
//

import CoreGraphics

/// Shared layout constants. One place to change, so nothing drifts out of
/// alignment between the header, the rows, and the floating bar.
enum Layout {
    static let screenPadding: CGFloat = 16
    static let maxContentWidth: CGFloat = 620
}
